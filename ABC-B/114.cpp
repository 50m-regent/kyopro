#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <numeric>
#include <map>
#include <set>
#include <bitset>
#include <iomanip>
#include <cmath>
#include <queue>
#define in std::cin
#define out std::cout
#define end std::endl
#define REP(i, A, N) for(int i=A;i<N;++i)
typedef long long int ll;

int main()
{
	string s;
	in >> s;
	REP(ll i, 0, sizeof(s))
	out << x + y / 2 << end;
	return 0;
}
